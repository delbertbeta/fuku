# Docker Environment Configuration for Fuku
# Copy this file to .env and adjust values as needed

# S3 Storage Configuration
# Uncomment and configure if using S3 storage
# S3_ACCESS_KEY_ID=your_access_key
# S3_SECRET_ACCESS_KEY=your_secret_key
# S3_BUCKET_NAME=fuku-images
# S3_REGION=us-east-1
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_CDN_URL=
# S3_PREFIX=uploads/

# Storage Type (local or s3)
IMAGE_STORAGE_TYPE=local

# Local upload directory (mounted from host)
LOCAL_UPLOAD_DIR=/data/uploads

# Session Secret (required - change this!)
SESSION_SECRET=change_this_to_a_secure_random_string

# Database Type (sqlite or mariadb)
DATABASE_TYPE=sqlite

# SQLite Configuration (when DATABASE_TYPE=sqlite)
# Database file will be stored in /data/fuku.db
DATABASE_PATH=/data/fuku.db

# MariaDB Configuration (when DATABASE_TYPE=mariadb)
# Uncomment and configure for external MariaDB
# Format: mysql://username:password@host:port/database
# MARIADB_URL=mysql://fuku_user:secure_password@host.docker.internal:3306/fuku_db

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Registration Control
ALLOW_REGISTRATION=true
