# Environment Variables

# S3 Storage Configuration
# Uncomment and configure when using S3 storage
# S3_ACCESS_KEY_ID=your_access_key
# S3_SECRET_ACCESS_KEY=your_secret_key
# S3_BUCKET_NAME=fuku-images
# S3_REGION=us-east-1
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_CDN_URL=
# S3_PREFIX=uploads/

# Storage Type (s3 or local)
IMAGE_STORAGE_TYPE=local

# Local upload directory
# For local development: ./public/uploads
# For Docker: /data/uploads (mounted from host)
LOCAL_UPLOAD_DIR=./public/uploads

# Session Secret (required)
# Use a strong random string: openssl rand -base64 32
SESSION_SECRET=your_session_secret_here

# Database
# Database Type (sqlite or mariadb)
DATABASE_TYPE=sqlite

# SQLite Configuration (when DATABASE_TYPE=sqlite)
# For local development: ./data/fuku.db
# For Docker: /data/fuku.db (mounted from host)
DATABASE_PATH=./data/fuku.db

# MariaDB Configuration (when DATABASE_TYPE=mariadb)
# Format: mysql://username:password@host:port/database
# For Docker with MariaDB on host: use host.docker.internal instead of localhost
# Example: MARIADB_URL=mysql://fuku_user:secure_password@host.docker.internal:3306/fuku_db
MARIADB_URL=mysql://outfit_user:secure_password@localhost:3306/outfit_platform

# Application URL
# Update to your actual domain in production
# For Docker with custom ports, ensure this matches the exposed port
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Registration Control
# Set to false in production to disable public registration
ALLOW_REGISTRATION=true
